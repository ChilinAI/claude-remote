# Claude Remote — Changelog

## 2026-02-18

### Fix: предотвращение App Nap и обработка пробуждения (v0.3.4)

**Файлы:** `app/src-tauri/Cargo.toml`, `app/src-tauri/src/lib.rs`, `app/src-tauri/tauri.conf.json`

**Проблема:** Через некоторое время после закрытия крышки MacBook (или простоя) демон переставал отвечать на сообщения. Причина: macOS включал **App Nap** для приложения со скрытым окном, что троттлило таймеры (2-секундный polling растягивался до минут) и приостанавливало сетевую активность.

**Что сделано:**
- Добавлена зависимость `objc = "0.2"` для macOS
- Функция `disable_app_nap()` — вызывает `NSProcessInfo.beginActivityWithOptions` с флагами `NSActivityIdleSystemSleepDisabled | NSActivityUserInitiatedAllowingIdleSystemSleep`, запрещая macOS троттлить приложение
- Обнаружение пробуждения в `poll_messages`: если между итерациями прошло >10 сек (вместо ожидаемых 2), принудительно обновляется Firebase токен и создаётся новый HTTP клиент (для избежания stale connections)
- Обнаружение пробуждения в `heartbeat_loop`: если между heartbeat-ами прошло >90 сек (вместо ожидаемых 30), принудительное обновление токена
- Вынесена функция `force_token_refresh()` для переиспользования
- Версия: 0.3.4

---

## 2026-02-17

### Fix: защита от запуска двух экземпляров (single instance)

**Файлы:** `app/src-tauri/Cargo.toml`, `app/src-tauri/src/lib.rs`

**Проблема:** При случайном двойном запуске приложения работали два daemon-а одновременно, что приводило к конфликтам с RTDB и некорректной работе.

**Что сделано:**
- Добавлен `tauri-plugin-single-instance` — второй экземпляр не запускается, а показывает окно первого
- Версия: 0.3.3

---

### Fix: heartbeat показывает offline через ~1 час

**Файл:** `app/src-tauri/src/lib.rs`

**Проблема:** Firebase ID токен истекает через 1 час. Функция `send_heartbeat` не обрабатывала ответ 401 и не обновляла токен. В результате heartbeat переставал записываться в RTDB, и веб-чат показывал статус "offline" (>90 сек без heartbeat), хотя приложение продолжало работать (poll_messages обновлял токен сам).

**Что сделано:**
- Добавлена обработка HTTP 401 в `send_heartbeat()` — при протухшем токене вызывается `refresh_auth_token()`, обновляются `auth_token` и `refresh_token` в state и на диске
- Следующий цикл heartbeat (через 30 сек) отправится уже с новым токеном

---

### Fix: авто-создание новой E2E сессии при отправке сообщения

**Файл:** `web/public/chat.html`

**Проблема:** Когда в чате были нерасшифрованные сообщения от предыдущей сессии (другие ключи), отправить новое сообщение было невозможно — в строке ввода отображалось предупреждение "Нажмите Очистить чат", и пользователю приходилось вручную нажимать кнопку "Clear Chat".

**Что сделано:**
- `sendMessage()` теперь автоматически создаёт новую сессию с новым обменом ключами ECDH, если шифрование не готово (encryptionReady = false)
- Старые нерасшифрованные сообщения остаются видимыми в окне чата (размытые), окно не очищается
- Убран `showEncryptedHint()` и все i18n ключи `encryptedHint` — предупреждение в placeholder больше не нужно
- После авто-создания сессии placeholder возвращается к стандартному тексту

---

### Fix: строка ввода прячется за клавиатуру на мобильных

**Файлы:** `web/public/chat.html`

**Что сделано:**
- Добавлен обработчик `visualViewport` API (resize/scroll) — при появлении виртуальной клавиатуры на iOS/Android высота чата динамически подстраивается под видимую область, строка ввода остаётся видимой
- `chatScreen` переведён на `position: fixed` (top/left/right/bottom: 0) для корректной работы с `visualViewport.height` и `transform: translateY()`
- При изменении viewport автоматически скроллит сообщения к последнему

### Fix: hero-изображение сжимается непропорционально на мобильных

**Файлы:** `web/public/index.html` + все 11 языковых версий (`ru`, `es`, `pt`, `fr`, `de`, `ja`, `ko`, `zh`, `hi`, `tr`, `uk`)

**Что сделано:**
- Добавлено `height: auto` к `.hero-image img` — изображение теперь масштабируется пропорционально на мобильных устройствах (ранее высота была зафиксирована HTML-атрибутом `height="380"`, что приводило к сжатию)

### Feature cards: inline icon+title layout на лендингах

**Файлы:** `web/public/{ru,es,pt,fr,de,ja,ko,zh,hi,tr,uk}/index.html` (все 11 языковых версий)

**Что сделано:**
- **CSS:** Заменены стили `.feature-icon` (neumorphic 48x48 блок) и `.feature h3` на inline-layout: новый `.feature-header` (flex, gap 12px), иконка 28px без фона, h3 без margin
- **HTML:** Иконка и заголовок в каждой feature-карточке обёрнуты в `<div class="feature-header">` — теперь отображаются в одну строку вместо вертикального стека

### E2E шифрование сообщений (ECDH P-256 + AES-256-GCM)

**Файлы:** `web/public/chat.html`, `app/src-tauri/src/lib.rs`, `app/src-tauri/Cargo.toml`

**Что сделано:**
- **Браузер (chat.html):** Web Crypto API — генерация ECDH P-256 keypair при создании/загрузке сессии, запись public key в RTDB (`sessions/{uid}/{sessionId}/keys/browser`), ожидание daemon public key, derivation AES-256-GCM shared key, шифрование исходящих и дешифрование входящих сообщений
- **Tauri daemon (lib.rs):** При обнаружении browser public key в сессии — генерация своего ECDH keypair, публикация daemon public key в RTDB, derivation того же AES-256-GCM ключа, дешифрование пользовательских сообщений перед отправкой в Claude, шифрование ответов
- **Backward compatibility:** поле `encrypted: true` + `iv` в сообщениях — старые незашифрованные сообщения рендерятся как есть
- **UI:** бейдж "E2E" с замком в header чата, появляется после успешного обмена ключами
- **Cargo.toml:** добавлены `p256`, `aes-gcm`, `base64`, `rand`

**Как работает:**
1. Браузер создаёт ECDH keypair, пишет public key в RTDB
2. Daemon видит browser key, создаёт свой keypair, пишет daemon key, вычисляет shared secret
3. Браузер видит daemon key, вычисляет тот же shared secret
4. Все сообщения шифруются AES-256-GCM с уникальным IV/nonce
5. В RTDB хранятся только зашифрованные данные + публичные ключи

---

## 2026-02-16 (ночь 2)

### Исправлен пустой dropdown языков на мобильном + добавлен испанский в чат

**Файлы:** `web/public/index.html`, `web/public/ru/index.html`, `web/public/es/index.html`, `web/public/chat.html`

**Что сделано:**
- **Баг: пустой dropdown на мобильном** — CSS-правило `.nav-links a:not(...)` скрывало ВСЕ ссылки включая внутренние ссылки dropdown. Исправлено на `.nav-links > a:not(...)` (только прямые потомки) на всех 3 лендингах (en/ru/es)
- **chat.html: добавлен испанский язык** — Español в dropdown, полный набор i18n переводов (18 строк), обновлён detectLang (поддержка es), updateLangHighlight (подсветка es), TTS fallback (es-ES)

---

## 2026-02-16 (ночь)

### Глобус-кнопка выбора языка вместо отдельных кнопок

**Файлы:** `web/public/index.html`, `web/public/ru/index.html`, `web/public/es/index.html`, `web/public/chat.html`

**Что сделано:** Заменены отдельные кнопки переключения языка (RU, ES, EN) на единую кнопку с иконкой глобуса. При клике раскрывается выпадающий список с доступными языками (English, Русский, Español). Текущий язык подсвечивается акцентным цветом. Список закрывается при клике вне него. В мобильной версии чата глобус работает внутри гамбургер-меню.

---

## 2026-02-16 (вечер)

### Испанская версия лендинга

**Файлы:**
- `web/public/es/index.html` — новый файл, испанский лендинг
- `web/public/index.html` — добавлен hreflang es, кнопка ES, авто-определение испанского языка
- `web/public/ru/index.html` — добавлен hreflang es, кнопка ES, авто-определение испанского языка

**Что сделано:** Добавлена испанская версия лендинга `/es/`. Все три страницы (EN, RU, ES) теперь имеют переключатели языков и авто-определение: если язык браузера `es*` — редирект на `/es/`, `ru*` — на `/ru/`, остальные — на `/`.

---

## 2026-02-17

### v0.2.9 — Сборка и публикация фикса --resume → --continue

- Версия поднята с 0.2.8 до 0.2.9
- Собрано с подписью, обновлён `latest.json`, задеплоено на Firebase Hosting
- Приложение в `/Applications/` заменено на новую сборку

---

## 2026-02-16

### Фикс: --resume → --continue в Claude Code runner

**Файл:** `app/src-tauri/src/lib.rs`

**Что сделано:** Заменён флаг `--resume` на `--continue` при вызове Claude Code. Новые версии Claude Code требуют указания session ID при `--resume` с `-p`, что вызывало ошибку: `Error: --resume requires a valid session ID when used with --print`. Флаг `--continue` продолжает последнюю сессию в рабочей директории без необходимости указывать ID.

---

### Гамбургер-меню для мобильной версии чата

**Файл:** `web/public/chat.html`

**Что сделано:** Кнопки TTS, RU, Clear Chat и Logout на экранах ≤600px скрываются в гамбургер-меню (☰). На десктопе всё без изменений. Меню закрывается при клике по оверлею.

---

### v0.2.3 — Автообновление, показ версии, фикс зависания демона

**Файлы:** `app/src-tauri/src/lib.rs`, `app/src/index.html`, `app/src-tauri/tauri.conf.json`

**Что сделано:**
1. **Фикс зависания демона** — демон переставал отвечать когда Firebase id_token истекал во время работы Claude Code. Теперь: подхватывает зависшие "processing" сообщения + refresh token перед записью ответа
2. **Автообновление (Tauri updater)** — перегенерированы minisign ключи, включён `createUpdaterArtifacts`, подписанный `.app.tar.gz` деплоится на хостинг. Приложение проверяет обновления при запуске и автоматически перезапускается после установки (`app_handle.restart()`)
3. **Отображение версии** — номер версии серым текстом справа от "LOG" в окне приложения (команда `get_version`)

---

### Улучшение качества TTS в веб-чате

**Файл:** `web/public/chat.html`

**Проблема:** Голос при озвучивании сообщений звучал неестественно. Использовалась модель OpenAI `tts-1` (оптимизирована для скорости, не качества) + browser SpeechSynthesis как фолбэк.

**Что сделано:**
1. Обновлена модель OpenAI с `tts-1` на `gpt-4o-mini-tts` (новейшая, лучшее качество голоса)
2. Добавлен провайдер **ElevenLabs** (`eleven_multilingual_v2`) — один из лучших по натуральности голоса, доступен бесплатно через Puter.js
3. Добавлен селектор TTS провайдера в хедере чата (OpenAI / ElevenLabs), выбор сохраняется в localStorage
4. Упрощён UI: вместо двух кнопок (Puter + Browser) теперь одна кнопка 🔊, провайдер выбирается в хедере
5. Browser SpeechSynthesis оставлен как фолбэк при ошибке API

**Результат:** Значительно улучшено качество голоса. Пользователь может выбрать между OpenAI и ElevenLabs. Задеплоено на https://clauderemote.web.app

---

### Исправление зависания демона при истечении Firebase token

**Файл:** `app/src-tauri/src/lib.rs`

**Проблема:** Демон зависал — переставал отвечать на сообщения из веб-чата. Причина: Firebase id_token (живёт ~1 час) истекал пока Claude Code выполнял задачу. После этого PUT/POST запросы на запись ответа молча фейлились (`let _ =`), сообщение оставалось в статусе "processing" навсегда и больше не подхватывалось (фильтр `status == "pending"`).

**Что сделано:**
1. Демон теперь подхватывает "processing" сообщения, если не занят (busy=false) — это значит они зависли от предыдущего неудачного выполнения
2. Перед записью ответа Claude демон проверяет валидность token и делает refresh если нужно — так POST/PUT проходят даже после долгого выполнения Claude

**Результат:** Демон больше не зависает при истечении token во время работы Claude Code.

---

### Ad-hoc code signing + инструкция по установке

**Файлы:** `app/src-tauri/tauri.conf.json`, `web/public/index.html`, `web/public/ru/index.html`

**Что сделано:**
- Настроен ad-hoc signing в tauri.conf.json (`macOS.signingIdentity: "-"`) — позволяет распространять без Apple Developer подписки ($99/год)
- Добавлен DMG в bundle targets (раньше был только .app)
- Собран новый DMG с ad-hoc подписью, загружен в `web/public/releases/`
- Ссылки на скачивание обновлены с .pkg на .dmg на обоих лендингах
- Добавлена секция "Installation" (EN) / "Установка" (RU) с 3 шагами: открыть DMG, правый клик → Open, или xattr -cr через Terminal
- Добавлена ссылка "Install"/"Установка" в навигацию обоих лендингов
- Задеплоено на Firebase Hosting

**Ограничения ad-hoc signing:**
- Пользователям нужно правый клик → Open (первый раз) или xattr -cr в терминале
- Нет нотаризации — macOS предупреждает о неизвестном разработчике

## 2026-02-15

### Лендинг переделан в стиле неоморфизма (как чат)

**Файлы:** `web/public/index.html`, `web/public/ru/index.html`

**Что сделано:**
- Заменён cyberpunk-стиль (тёмно-синий фон, красный акцент `#e94560`, glow-эффекты, градиенты) на неоморфизм из `chat.html`
- CSS переменные: `--bg: #2d2d2d`, `--surface: #333333`, `--accent: #d4764e` (тёплый оранжевый)
- Добавлена поддержка светлой темы через `prefers-color-scheme: light`
- Навигация: вместо blur-фона — neumorphic кнопки с мягкими тенями
- Кнопки: neumorphic push-in эффект при нажатии (inset тени)
- Карточки (steps, features): выпуклые с двойными тенями вместо gradient+border
- Иконки фич: inset-контейнеры вместо обычного текста
- Номера шагов: inset-круги с accent-цветом вместо залитых кругов
- Изображение героя: neumorphic тени вместо drop-shadow
- Убран `backdrop-filter: blur()` из навигации
- Единый визуальный стиль между лендингом и чатом

### Улучшено качество TTS — Puter.js (OpenAI TTS, голос nova)

**Файл:** `web/public/chat.html`

**Что сделано:**
- Заменён стандартный browser `SpeechSynthesis` на **Puter.js** (`https://js.puter.com/v2/`) — бесплатный доступ к OpenAI TTS без API ключей
- Модель: `tts-1`, голос: `nova` (естественно звучащий, мультиязычный — поддерживает EN и RU)
- Puter.js загружается лениво (lazy-load) при первом нажатии на кнопку 🔊
- При ошибке Puter TTS — автоматический фолбэк на встроенный browser SpeechSynthesis
- Лимит текста: 3000 символов (ограничение Puter API)
- Повторное нажатие на кнопку останавливает воспроизведение

**Почему не Edge TTS:** ранее edge-tts-universal не работал из-за WebSocket блокировки `wss://speech.platform.bing.com` (см. историю ниже)

**Задеплоено:** https://chilin1.web.app

---

### Добавлен мониторинг здоровья демона (Heartbeat)

**Файлы:** `app/src-tauri/src/lib.rs`, `app/src-tauri/Cargo.toml`, `web/public/chat.html`

**Что сделано:**
- Tauri daemon отправляет heartbeat в RTDB каждые ~30 сек (`/daemon_status/{uid}`)
- Heartbeat содержит: status (idle/busy/stopped), hostname, lastHeartbeat timestamp
- При обработке сообщения отправляется heartbeat со статусом "busy", после — "idle"
- В веб-чате в хедере отображается реальный статус демона:
  - Зелёный "connected" — демон работает, idle
  - Оранжевый "busy" — обрабатывает запрос
  - Красный "offline" — heartbeat не приходил >90 сек или демон остановлен
- Добавлена зависимость `hostname = "0.4"` в Cargo.toml
- Добавлены i18n ключи для RU/EN (busy, offline, processing и т.д.)
- Пересобрано приложение, задеплоен сайт на chilin1.web.app

---

### Удалена кнопка Edge TTS (Microsoft)

**Файл:** `web/public/chat.html`

**Что сделано:**
- Удалена левая кнопка 🗣️ Edge TTS (Microsoft Neural Voices)
- Удалены: CSS стили `.edge-tts-btn`, JS код `toggleEdgeTTS`, `loadEdgeTTS`, переменные `edgeTTSAudio`, `currentEdgeBtn`, `EdgeTTSModule`
- Кнопка Puter TTS сдвинута на позицию `left: 8px` (на место удалённой)
- Убраны ссылки на Edge TTS из функции `togglePuterTTS`

**Оставшиеся кнопки TTS (слева направо):**
1. 🎙️ Puter TTS (синяя) — OpenAI TTS через Puter.js
2. 🔊 Browser TTS (красная) — встроенный speechSynthesis

---

### Добавлена третья кнопка TTS — Puter.js (OpenAI voice)

**Файл:** `web/public/chat.html`

**Что сделано:**
- Добавлена третья кнопка 🎙️ (синяя) рядом с существующими кнопками TTS
- Использует **Puter.js** (`https://js.puter.com/v2/`) — бесплатный доступ к OpenAI TTS без API ключей
- Модель: `tts-1`, голос: `nova` (мультиязычный, поддерживает русский и английский)
- Автоматически определяет язык текста (OpenAI TTS мультиязычная модель)
- При ошибке Puter TTS — фолбэк на встроенный browser TTS
- Лимит текста: 3000 символов (ограничение Puter API)

**Расположение кнопок (слева направо):**
1. 🗣️ Edge TTS (зелёная) — Microsoft Neural Voices (может не работать из-за WebSocket блокировки)
2. 🎙️ Puter TTS (синяя) — OpenAI TTS через Puter.js (новая, рабочая)
3. 🔊 Browser TTS (красная) — встроенный speechSynthesis

**Задеплоено:** https://chilin1.web.app

---

### Исследование: Кнопка Edge TTS (🗣️) не работает

**Файл:** `web/public/chat.html`

**Описание проблемы:** Кнопка с динамиком (🗣️) слева от сообщений ассистента — использует библиотеку `edge-tts-universal` для озвучки через Microsoft Neural Voices. Кнопка не работает.

**Найденные причины:**
1. **Silent audio.play() перед загрузкой src** (строки 465-467) — создаётся `new Audio()` и вызывается `.play()` без источника для "unlock audio context" на iOS. Это вызывает ошибку `AbortError: The play() request was interrupted` в Chrome/Safari. Хотя ошибка ловится через `.catch()`, она может повлиять на дальнейшее воспроизведение.
2. **WebSocket блокировка** — `edge-tts-universal` использует WebSocket к `wss://speech.platform.bing.com/consumer/speech/synthesize/readaloud/edge/v1`. Этот endpoint может блокировать запросы с определённых origin'ов или требовать определённые заголовки, которые невозможно установить из браузера.
3. **Отсутствие видимой ошибки для пользователя** — при ошибке кнопка молча фолбэчит на browser TTS (строка 506), который может тоже не работать если не загружены голоса.

**Исправления:**
- Убрана попытка `audio.play()` без src (audio unlock context)
- Добавлен user-visible feedback при ошибке (кнопка мигает красным)
- Добавлен таймаут 15 сек на synthesize() чтобы не зависало
- Улучшен error handling в catch блоке
- Добавлен console.log при начале загрузки для отладки
